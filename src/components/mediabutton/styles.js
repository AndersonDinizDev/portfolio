import styled from "styled-components";

export const MediaButton = styled.button`
  transition: all 0.5s;
  cursor: pointer;
  background: none;
  border: none;

  &:hover {
    transform: scale(1.0);
  }
`;
